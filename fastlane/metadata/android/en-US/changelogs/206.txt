- Deep audit pass (engineering + UX)
- Export cancellation is now honored across all phases. Cancelling during the FFmpeg reversed-clip pre-render previously kept rendering every queued clip, then launched a detached full encode whose state-guarded listener never fired - leaking reversed-clip temp files in cache and burning CPU/battery to the end of the encode. The pre-render loop now stops between clips, the transformer refuses to start after cancellation, and a cancel during encoding signa...
