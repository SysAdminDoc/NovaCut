- Math correctness
- KeyframeEngine.getValueAt clamps OPACITY and VOLUME to safe ranges - Bezier curves with handles outside the unit square (and the ELASTIC / BACK / SPRING easings) can legitimately overshoot [0, 1]. For position / scale / rotation the overshoot is the desired effect (springy motion); for OPACITY and VOLUME it's a contract violation: opacity < 0 means "less than transparent", opacity > 1 brightens via RgbMatrix, and negative volume in VolumeAudioProcessor inverts phase. A n...
